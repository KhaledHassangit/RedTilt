// eslint-disable-next-line react/prop-types
const SectionTitle = ({ title }) => {
    return (
        <h2 className="text-4xl font-bold text-white mb-4" role="heading">
            {title}
        </h2>
    );
};

export default SectionTitle;
